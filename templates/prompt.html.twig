<!DOCTYPE html>
<html lang="pl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Witaj w naszym systemie{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
        <style>
            body {
                background: linear-gradient(45deg, #f3f4f7, #d1e7ff);
                font-family: 'Arial', sans-serif;
            }

            .container {
                margin-top: 50px;
            }

            .response-container {
                background: #ffffff;
                border-radius: 10px;
                padding: 30px;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            .response-container h2 {
                color: #0056b3;
                font-size: 28px;
                font-weight: bold;
            }

            .response-content {
                background: #f0f9ff;
                border-left: 6px solid #0056b3;
                padding: 20px;
                font-size: 18px;
                color: #333;
                line-height: 1.6;
                border-radius: 8px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }

            .response-content p {
                margin-bottom: 0;
            }

            .response-container .btn {
                width: 100%;
                padding: 12px;
                font-size: 18px;
                background-color: #007bff;
                border-color: #007bff;
                border-radius: 5px;
                transition: all 0.3s ease;
            }

            .response-container .btn:hover {
                background-color: #0056b3;
                border-color: #004085;
            }

            .alert {
                margin-top: 20px;
            }

            .fadeIn {
                animation: fadeIn 1.5s ease-in-out;
            }

            @keyframes fadeIn {
                from {
                    opacity: 0;
                }
                to {
                    opacity: 1;
                }
            }
        </style>
    {% endblock %}
</head>

<body>
{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="response-container fadeIn">
                    <h2>Odpowiedź na Twój prompt</h2>
                    <div class="response-content">
                        <p>{{ response.content }}</p>
                    </div>
                    <a href="#" class="btn btn-primary mt-4">Nowy prompt</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
{% endblock %}
</body>

</html>
